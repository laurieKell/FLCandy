% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stockTrend.R
\name{stockTrend}
\alias{stockTrend}
\alias{stockTrend,FLStock,missing,missing-method}
\alias{stockTrend,FLQuant,missing,missing-method}
\alias{stockTrend,missing,FLQuant,FLQuant-method}
\alias{stockTrend,data.frame,missing,missing-method}
\title{Analyze Stock Trends: SSB and F Trajectories}
\usage{
stockTrend(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  pThreshold = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE
)

\S4method{stockTrend}{FLStock,missing,missing}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  pThreshold = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE
)

\S4method{stockTrend}{FLQuant,missing,missing}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  pThreshold = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE
)

\S4method{stockTrend}{missing,FLQuant,FLQuant}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  pThreshold = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE
)

\S4method{stockTrend}{data.frame,missing,missing}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  pThreshold = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE
)
}
\arguments{
\item{object}{An FLStock object, FLQuant object, or a data frame with columns 'ssb', 'f', and 'year'}

\item{ssb}{An FLQuant object for spawning stock biomass (optional if object is FLStock)}

\item{f}{An FLQuant object for fishing mortality (optional if object is FLStock)}

\item{method}{Method for trend analysis: "linear" (linear regression) or "mann_kendall" (Mann-Kendall test)}

\item{pThreshold}{Significance threshold for trend detection (default: 0.05)}

\item{minYears}{Minimum number of years required for analysis (default: 5)}

\item{recentYears}{Number of recent years to focus on (default: NULL, uses all data)}

\item{windowSize}{Size of sliding window for year-by-year classification (default: 5)}

\item{byYear}{Logical. If TRUE, returns classification for each year using sliding window. If FALSE, returns overall trend.}
}
\value{
A list containing:
\itemize{
\item \code{trend}: Character string indicating overall trend ("Increasing", "Declining", "Mixed", "Stable")
\item \code{yearlyStates}: Data frame with year-by-year classifications (if byYear=TRUE)
\item \code{ssbTrend}: SSB trend analysis results
\item \code{fTrend}: F trend analysis results
\item \code{classification}: Detailed classification with reasoning
\item \code{plotData}: Data frame with standardized values for plotting
}
}
\description{
Determines if a stock is declining or increasing based on the trends in
spawning stock biomass (SSB) and fishing mortality (F). A stock is considered:
\itemize{
\item \strong{Increasing}: SSB is increasing while F is decreasing
\item \strong{Declining}: SSB is decreasing while F is increasing
\item \strong{Mixed}: Other combinations (SSB increasing with F increasing, or SSB decreasing with F decreasing)
\item \strong{Stable}: No significant trend in either variable
}
}
\examples{
\dontrun{
# Using FLStock object
data(ple4)
result <- stockTrend(ple4, byYear=TRUE)
print(result$trend)
print(result$yearlyStates)

# Using FLQuant objects
ssbQuant <- ssb(ple4)
fQuant <- fbar(ple4)
result <- stockTrend(ssb = ssbQuant, f = fQuant, byYear=TRUE)

# Using data frame
tsData <- data.frame(
  year = 2000:2020,
  ssb = rnorm(21, 100, 10),
  f = rnorm(21, 0.3, 0.05)
)
result <- stockTrend(tsData, byYear=TRUE)
}

}
