% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stockState.R
\name{stockState}
\alias{stockState}
\alias{stockState,FLStock,missing,missing-method}
\alias{stockState,FLQuant,missing,missing-method}
\alias{stockState,missing,FLQuant,FLQuant-method}
\alias{stockState,data.frame,missing,missing-method}
\title{Identify Stock State}
\usage{
stockState(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  p = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE,
  simplify = TRUE
)

\S4method{stockState}{FLStock,missing,missing}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  p = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE,
  simplify = TRUE
)

\S4method{stockState}{FLQuant,missing,missing}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  p = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE,
  simplify = TRUE
)

\S4method{stockState}{missing,FLQuant,FLQuant}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  p = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE,
  simplify = TRUE
)

\S4method{stockState}{data.frame,missing,missing}(
  object,
  ssb = NULL,
  f = NULL,
  method = "linear",
  p = 0.05,
  minYears = 5,
  recentYears = NULL,
  windowSize = 5,
  byYear = FALSE,
  simplify = TRUE
)
}
\arguments{
\item{object}{An FLStock object, FLQuant object, data frame with columns 'ssb', 'f', 'year',
or a list of FLStock/FLQuant objects}

\item{ssb}{An FLQuant object for spawning stock biomass (optional if object is FLStock)}

\item{f}{An FLQuant object for fishing mortality (optional if object is FLStock)}

\item{method}{Method for trend analysis: "linear" (linear regression) or "mann_kendall" (Mann-Kendall test)}

\item{p}{Significance threshold for trend detection (default: 0.05)}

\item{minYears}{Minimum number of years required for analysis (default: 5)}

\item{recentYears}{Number of recent years to focus on (default: NULL, uses all data)}

\item{windowSize}{Size of sliding window for year-by-year classification (default: 5)}

\item{byYear}{Logical. If TRUE, returns classification for each year using sliding window. If FALSE, returns overall trend.}

\item{simplify}{Logical. If TRUE, returns only the state classification. If FALSE, returns detailed results.}
}
\value{
If \code{simplify=TRUE}: A character vector with states ("Increasing", "Decreasing", "Stable")
If \code{simplify=FALSE}: A list with detailed results for each stock
}
\description{
Creates a variable that identifies stock state as increasing, decreasing, or stable
based on trends in SSB and F. Provides a simple classification that can be used for
grouping and filtering.
}
\examples{
\dontrun{
# Single stock
data(ple4)
state=stockState(ple4)
print(state)  # "Increasing"

# Using FLQuant objects
ssbQuant=ssb(ple4)
fQuant=fbar(ple4)
state=stockState(ssb=ssbQuant, f=fQuant)

# Multiple stocks
stocks=list(ple4=ple4, ple4_2=ple4)
states=stockState(stocks)
print(states)  # c("Increasing", "Increasing")

# With detailed results
results=stockState(ple4, simplify=FALSE)
print(results$trend)

# Year-by-year classification
yearlyResults=stockState(ple4, byYear=TRUE)
print(yearlyResults$yearlyStates)
}

}
